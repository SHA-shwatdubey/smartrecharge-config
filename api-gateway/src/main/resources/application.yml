spring:
  application:
    name: api-gateway
  config:
    import: optional:configserver:http://localhost:8060

server:
  port: 8062

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway

